import React                from 'react'
import ReactDOM             from 'react-dom'

import App                  from './app'

const root = document.getElementById('react-root')

ReactDOM.render(
    <App />,
root)